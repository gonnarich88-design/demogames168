BOT_TOKEN=YOUR_BOT_TOKEN_HERE
WEBAPP_URL=http://localhost:3000
PORT=3000
NODE_ENV=development

# Deploy ด้วย docker-compose: volume app-data mount ที่ /app/data อยู่แล้ว — ข้อมูลสถิติไม่หาย
# หรือกำหนด path เอง: BOT_EVENTS_PATH=/app/data/bot-events.json

# Optional: outbound proxy to bypass Thailand block on jiligames.com
# OUTBOUND_PROXY_URL=http://user:pass@proxy-host:port
